/**
 * 160 Benchmark IT Solutions Technical / TITA Questions
 * Genuine free-text questions with expectedAnswer, explanation, betterAnswer.
 * Difficulty: ~37.5% Easy (60), ~47.5% Medium (76), ~15% Hard (24).
 * Marks: Easy=2, Medium=3, Hard=5.
 * Strictly NO resume / project / personal questions.
 */

export const technicalQuestions = [
  // ── 1. DSA: Sorting, Searching, Complexity & Algorithms (Q1 - Q40) ──
  {
    questionId: "benchmark-tech-001",
    question: "What are the major sorting algorithms available and which one is generally considered the fastest in average-case scenarios?",
    expectedAnswer: "Common sorting algorithms include Quick Sort, Merge Sort, Heap Sort, Insertion Sort, Bubble Sort, and Selection Sort. In average-case practical scenarios, Quick Sort (O(n log n)) is generally considered the fastest due to its small constant factors, in-place partitioning, and excellent CPU cache locality.",
    explanation: "Although Quick Sort, Merge Sort, and Heap Sort all have O(n log n) average time complexity, Quick Sort minimizes memory moves and benefits from hardware cache lines.",
    betterAnswer: "The primary sorting algorithms are Quick Sort, Merge Sort, Heap Sort (all O(n log n) average time), and quadratic sorts like Insertion Sort, Selection Sort, and Bubble Sort (O(n^2)). Quick Sort is practically the fastest for general-purpose in-memory sorting because its tight inner loops utilize CPU cache locality efficiently and it operates in-place with O(log n) auxiliary stack space, despite an O(n^2) worst-case when unbalanced pivots occur.",
    difficulty: "Easy",
    marks: 2,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Sorting Algorithms"
  },
  {
    questionId: "benchmark-tech-002",
    question: "Explain the difference between stable and unstable sorting algorithms with examples.",
    expectedAnswer: "A stable sorting algorithm preserves the relative original order of equal elements in the sorted output (e.g., Merge Sort, Insertion Sort). An unstable sorting algorithm may change the relative order of duplicate elements during swaps (e.g., Quick Sort, Heap Sort, Selection Sort).",
    explanation: "Stability is critical when sorting records by multiple criteria sequentially (e.g., sorting by name, then by age).",
    betterAnswer: "A sorting algorithm is stable if two elements with equal keys appear in the same relative order in the sorted output as they were in the initial input. Merge Sort, Insertion Sort, Bubble Sort, and TimSort are stable. Quick Sort, Heap Sort, and Selection Sort are unstable because long-distance element swaps can disrupt the initial ordering of duplicate keys. Stability is essential in multi-column sorting pipelines.",
    difficulty: "Easy",
    marks: 2,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Sorting Algorithms"
  },
  {
    questionId: "benchmark-tech-003",
    question: "What causes the worst-case time complexity in Quick Sort, and how can it be mitigated?",
    expectedAnswer: "Quick Sort reaches its worst-case time complexity of O(n^2) when the chosen pivot always results in maximally unbalanced partitions (such as choosing the first or last element in an already sorted or reverse-sorted array). It is mitigated by using randomized pivot selection, median-of-three partitioning, or switching to Introsort.",
    explanation: "When partitions are of size 1 and n-1 at every recursive level, the recursion tree depth becomes n, resulting in n*(n-1)/2 comparisons.",
    betterAnswer: "Quick Sort degrades to O(n^2) time complexity when the partition step consistently divides an array of size k into subproblems of size 0 and k-1. This happens when the pivot is always the minimum or maximum element, typical when picking extreme elements on already sorted inputs. Mitigations include: (1) Median-of-Three pivot selection (taking median of first, middle, last), (2) Randomized pivot picking, and (3) Dual-pivot Quick Sort / Introsort (which falls back to Heap Sort if recursion depth exceeds 2*log n).",
    difficulty: "Medium",
    marks: 3,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Sorting Algorithms"
  },
  {
    questionId: "benchmark-tech-004",
    question: "When would you choose Merge Sort over Quick Sort in production software systems?",
    expectedAnswer: "Merge Sort is preferred when guaranteed worst-case O(n log n) time is required, when sorting linked lists (due to sequential access and O(1) extra space), when stability is strictly needed, or when performing external sorting on large datasets stored on disk where sequential disk I/O dominates.",
    explanation: "Merge Sort guarantees O(n log n) even on worst-case data and sorts linked lists without random access indexing.",
    betterAnswer: "Merge Sort is chosen over Quick Sort in several key scenarios: (1) When stable sorting is mandatory (e.g., sorting database records with secondary keys), (2) When worst-case time guarantees of O(n log n) are required for mission-critical/real-time systems, (3) When sorting linked lists, where pointers can be rearranged in O(1) auxiliary space without random access overhead, and (4) External sorting where datasets exceed RAM and must be streamed sequentially from disk.",
    difficulty: "Medium",
    marks: 3,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Sorting Algorithms"
  },
  {
    questionId: "benchmark-tech-005",
    question: "Explain the working principle and time/space complexity of Heap Sort.",
    expectedAnswer: "Heap Sort works in two phases: first, it builds a Max-Heap from the input array in O(n) time. Second, it repeatedly extracts the maximum element (root), swaps it with the last unsorted element, and heapifies down in O(log n) per element. Total time complexity is O(n log n) for best, average, and worst cases, with O(1) auxiliary space.",
    explanation: "Heap Sort utilizes a binary heap representation stored in a flat array where children of index i are at 2i+1 and 2i+2.",
    betterAnswer: "Heap Sort is an in-place comparison-based sorting algorithm using a complete binary tree (Binary Max-Heap). It converts the input array into a Max-Heap using bottom-up heapification in O(n) time. It then repeatedly swaps the root (maximum element) with the last element of the active heap, decreases heap size by 1, and restores the heap property via sift-down in O(log n). Total time is O(n log n) across all cases with O(1) auxiliary space, though it is unstable and has poorer cache locality than Quick Sort.",
    difficulty: "Medium",
    marks: 3,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "practice",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Sorting Algorithms"
  },
  {
    questionId: "benchmark-tech-006",
    question: "Compare the time and space complexity of Linear Search vs Binary Search.",
    expectedAnswer: "Linear Search checks every element sequentially, taking O(n) time in worst/average case and O(1) space, working on unsorted data. Binary Search repeatedly divides a sorted search space in half, taking O(log n) time and O(1) space iteratively (or O(log n) recursive stack space), requiring the collection to be sorted.",
    explanation: "Binary search eliminates half the remaining elements per comparison, yielding logarithmic time.",
    betterAnswer: "Linear Search operates on arbitrary (unsorted or sorted) sequences with O(1) best-case, O(n) average and worst-case time, and O(1) auxiliary space. Binary Search requires elements to be sorted with random access indexing, operating in O(1) best-case, O(log n) average and worst-case time, and O(1) auxiliary space (iterative). For n = 1,000,000, Linear Search needs up to 10^6 comparisons whereas Binary Search takes at most 20.",
    difficulty: "Easy",
    marks: 2,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "practice",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Searching Algorithms"
  },
  {
    questionId: "benchmark-tech-007",
    question: "Explain the difference between an Array and a Singly Linked List in terms of memory layout and operation complexities.",
    expectedAnswer: "An Array stores elements in contiguous memory locations allowing O(1) random access by index, but insertions/deletions take O(n) due to element shifting and resizing is expensive. A Linked List stores nodes scattered in memory linked by pointers, allowing O(1) insertions/deletions at known node positions, but requires O(n) sequential traversal to access elements by index and incurs extra pointer memory overhead.",
    explanation: "Contiguous allocation enables direct address arithmetic `base + index * size`, while linked nodes require pointer dereferencing.",
    betterAnswer: "Arrays use contiguous blocks of memory, offering O(1) constant-time random access via index arithmetic and superior cache locality, but have fixed capacity (requiring O(n) reallocation) and O(n) insertion/deletion due to shifting. Singly Linked Lists allocate nodes dynamically on the heap with pointers (`val, next`), offering dynamic resizing and O(1) insertions/deletions given a pointer to the position, but suffer from O(n) sequential lookup, lack of cache locality, and 4/8-byte pointer overhead per node.",
    difficulty: "Easy",
    marks: 2,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Data Structures"
  },
  {
    questionId: "benchmark-tech-008",
    question: "How do you detect a cycle in a linked list without modifying the nodes or using extra O(n) memory?",
    expectedAnswer: "Use Floyd's Cycle-Finding Algorithm (Tortoise and Hare). Initialize two pointers, slow and fast, at the head of the list. Move slow by one step and fast by two steps. If fast and slow meet at the same node, a cycle exists. If fast reaches null, no cycle exists. Time complexity is O(n) and auxiliary space is O(1).",
    explanation: "In a cycle of length C, the relative distance between slow and fast decreases by 1 in each step, guaranteeing they meet within C iterations.",
    betterAnswer: "Floyd's Tortoise and Hare algorithm detects cycles in O(n) time and O(1) space. Slow pointer advances by 1 node (`slow = slow.next`) while fast pointer advances by 2 nodes (`fast = fast.next.next`). If the list is acyclic, `fast` or `fast.next` reaches `null`. If a cycle exists, the relative gap between `fast` and `slow` closes by 1 node per iteration, ensuring they will intersect within one loop traversal of the cycle.",
    difficulty: "Medium",
    marks: 3,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Linked Lists"
  },
  {
    questionId: "benchmark-tech-009",
    question: "Explain the differences between Stack (LIFO) and Queue (FIFO) and describe two real-world software applications for each.",
    expectedAnswer: "A Stack follows Last-In-First-Out (LIFO) where push/pop happen at the top; applications include undo/redo mechanisms in text editors and call stack management in compilers/recursion. A Queue follows First-In-First-Out (FIFO) where enqueue happens at rear and dequeue at front; applications include CPU process scheduling (e.g. Round Robin) and printer spooling/message broker queues.",
    explanation: "Stacks reverse order while queues preserve arrival order.",
    betterAnswer: "A Stack is a LIFO (Last-In-First-Out) linear structure where insertions (push) and removals (pop) occur at the same end (top) in O(1) time. Key applications: (1) Execution call stack and recursion resolution, (2) Expression evaluation and syntax parsing (e.g. balanced brackets). A Queue is a FIFO (First-In-First-Out) structure where insertion occurs at the rear and deletion at the front in O(1) time. Key applications: (1) Asynchronous task queues (e.g., RabbitMQ, Kafka, printer spoolers), (2) Breadth-First Search (BFS) graph traversals.",
    difficulty: "Easy",
    marks: 2,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Stacks & Queues"
  },
  {
    questionId: "benchmark-tech-010",
    question: "How can you implement a Queue using two Stacks? Explain the time complexity of enqueue and dequeue operations.",
    expectedAnswer: "Use two stacks, `inputStack` and `outputStack`. For `enqueue(x)`, push onto `inputStack` (O(1)). For `dequeue()`, if `outputStack` is empty, pop all elements from `inputStack` and push them into `outputStack` (which reverses order to FIFO), then pop from `outputStack`. Amortized time complexity for dequeue is O(1) because each element is transferred exactly once.",
    explanation: "Double reversal turns LIFO into FIFO order.",
    betterAnswer: "To implement a Queue with two stacks (`inStack`, `outStack`): (1) `enqueue(x)` pushes `x` directly to `inStack` in O(1) time. (2) `dequeue()` checks if `outStack` is empty; if empty, it pops all items from `inStack` and pushes them into `outStack` in reversing order, then pops the top of `outStack`. While a single dequeue might take O(n) when transferring, each element is pushed to `inStack`, popped from `inStack`, pushed to `outStack`, and popped from `outStack` exactly once, giving an amortized O(1) time complexity per operation.",
    difficulty: "Medium",
    marks: 3,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "practice",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Stacks & Queues"
  },
  {
    questionId: "benchmark-tech-011",
    question: "Explain the difference between a Binary Tree and a Binary Search Tree (BST).",
    expectedAnswer: "A Binary Tree is a hierarchical tree where each node has at most two children with no ordering constraint. A Binary Search Tree (BST) is a binary tree with an ordering property: for every node, all values in its left subtree are strictly less than the node's value, and all values in its right subtree are strictly greater than the node's value.",
    explanation: "The BST ordering property enables O(log n) average-case search, insertion, and deletion.",
    betterAnswer: "A Binary Tree is a generic node-based structure where each node contains a value and at most two child pointers (`left`, `right`) without value ordering rules. A Binary Search Tree (BST) enforces the binary search property: for any node X, all keys in X's left subtree are strictly smaller than X.key, and all keys in X's right subtree are strictly greater than X.key. This allows binary search operations in O(h) time (where h is height: O(log n) average, O(n) worst-case for skewed trees), and an in-order traversal of a BST yields keys in strictly sorted ascending order.",
    difficulty: "Easy",
    marks: 2,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Trees & BST"
  },
  {
    questionId: "benchmark-tech-012",
    question: "What is the worst-case time complexity of searching in an unbalanced BST vs a Self-Balancing BST (like AVL or Red-Black Tree)?",
    expectedAnswer: "In an unbalanced BST, search can degrade to O(n) in the worst case when nodes form a skewed linear chain (like a linked list). In a Self-Balancing BST (AVL or Red-Black Tree), rotations keep tree height strictly balanced at O(log n), guaranteeing O(log n) search, insertion, and deletion in the worst case.",
    explanation: "AVL trees maintain height difference <= 1 between child subtrees, while Red-Black trees enforce color invariants preventing paths from exceeding twice the shortest path.",
    betterAnswer: "In an unbalanced standard BST, inserting sorted data (e.g. 1, 2, 3, 4, 5) results in a degenerate skewed tree of height n, making search O(n). In contrast, Self-Balancing BSTs (AVL, Red-Black Trees) perform tree rotations during insertions and deletions to maintain height h = O(log n). AVL trees strictly balance heights (balance factor in {-1, 0, 1}), while Red-Black Trees enforce color constraints ensuring the longest path is at most 2 * shortest path. Both guarantee O(log n) worst-case time for search, insert, and delete operations.",
    difficulty: "Medium",
    marks: 3,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "practice",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Trees & BST"
  },
  {
    questionId: "benchmark-tech-013",
    question: "Explain the four standard tree traversal techniques: Pre-order, In-order, Post-order, and Level-order.",
    expectedAnswer: "Pre-order visits Root -> Left -> Right (used for copying or serializing trees). In-order visits Left -> Root -> Right (produces sorted keys in a BST). Post-order visits Left -> Right -> Root (used for deleting or evaluating expression trees). Level-order visits nodes breadth-first level by level from top to bottom using a queue.",
    explanation: "Pre/In/Post are Depth-First Traversals (DFS) using recursion/stack, whereas Level-order is Breadth-First Traversal (BFS) using a FIFO queue.",
    betterAnswer: "Tree traversals systematically visit each node: (1) Pre-order (N-L-R): Visits current node, then left subtree, then right subtree; useful for cloning or prefix expressions. (2) In-order (L-N-R): Visits left subtree, current node, then right subtree; for a BST, this produces keys in non-decreasing order. (3) Post-order (L-R-N): Visits left subtree, right subtree, then current node; essential for bottom-up cleanup, deleting trees, and post-fix evaluation. (4) Level-order (BFS): Traverses level-by-level using a FIFO queue, finding shortest paths in unweighted trees.",
    difficulty: "Easy",
    marks: 2,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Trees & BST"
  },
  {
    questionId: "benchmark-tech-014",
    question: "What is a Hash Collision in Hash Tables, and what are the two primary collision resolution techniques?",
    expectedAnswer: "A Hash Collision occurs when two distinct keys produce the same hash index via the hash function. The two primary resolution techniques are: (1) Separate Chaining (each bucket holds a linked list or balanced tree of colliding entries), and (2) Open Addressing (searches for another empty bucket using linear probing, quadratic probing, or double hashing).",
    explanation: "Because hash tables have finite buckets (m) and infinite possible keys (k > m), the Pigeonhole Principle guarantees collisions.",
    betterAnswer: "A hash collision happens when hash(key1) == hash(key2) for key1 != key2. The two primary resolution strategies are: (1) Separate Chaining: Each hash table slot stores a pointer to a linked list (or Red-Black tree in Java 8+) holding all key-value pairs hashed to that bucket; handles load factors > 1 gracefully. (2) Open Addressing: All elements reside directly within the array table; on collision, it probes alternate slots using Linear Probing (hash + i), Quadratic Probing (hash + c1*i + c2*i^2), or Double Hashing (hash1 + i*hash2).",
    difficulty: "Medium",
    marks: 3,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Hashing"
  },
  {
    questionId: "benchmark-tech-015",
    question: "Explain the importance of the Base Case in recursive algorithms and what happens if it is omitted.",
    expectedAnswer: "The Base Case in recursion provides the terminating condition where the function returns a concrete result without making further recursive calls. If the base case is omitted or unreachable, the function enters infinite recursion, continually allocating stack frames until a StackOverflowError / runtime crash occurs.",
    explanation: "Every recursive call consumes stack memory for local variables, return address, and parameters.",
    betterAnswer: "The base case is the foundational stopping condition in a recursive algorithm that handles trivial inputs directly without making subsequent recursive calls. It breaks the recursive descent and initiates the unwinding phase. Omitting or improperly formulating a base case causes indefinite recursive calls, rapidly exhausting the OS thread call stack memory and triggering a `StackOverflowError` (or Segmentation Fault in C/C++).",
    difficulty: "Easy",
    marks: 2,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Recursion"
  },

  // ── 2. Object-Oriented Programming (OOP) (Q16 - Q50) ──
  {
    questionId: "benchmark-tech-016",
    question: "Explain the four core pillars of Object-Oriented Programming with practical examples.",
    expectedAnswer: "The four pillars of OOP are: (1) Encapsulation: Bundling data and methods while hiding internal state via private access modifiers and getters/setters. (2) Abstraction: Hiding implementation details and exposing only essential interfaces (using abstract classes/interfaces). (3) Inheritance: Deriving child classes from parent classes to promote code reuse (`class Dog extends Animal`). (4) Polymorphism: The ability for an entity to take multiple forms (method overloading at compile-time and method overriding at runtime).",
    explanation: "These pillars provide modularity, maintainability, extensibility, and data protection in software design.",
    betterAnswer: "The four fundamental OOP pillars are: (1) Encapsulation: Bundles object state (fields) and behaviors (methods), restricting unauthorized direct mutation via access modifiers (`private`, `protected`) to maintain object invariants. (2) Abstraction: Emphasizes what an object does rather than how it does it, decoupling interfaces from concrete implementations via `interface` and `abstract class`. (3) Inheritance: Creates an 'is-a' relationship where subclasses inherit and extend properties and behaviors from superclasses, maximizing code reuse. (4) Polymorphism: Enables uniform interfaces for distinct underlying data types via static polymorphism (compile-time method overloading) and dynamic polymorphism (runtime method overriding via virtual method tables).",
    difficulty: "Easy",
    marks: 2,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "OOP Concepts"
  },
  {
    questionId: "benchmark-tech-017",
    question: "What is the difference between Method Overloading and Method Overriding?",
    expectedAnswer: "Method Overloading occurs within the same class where methods share the same name but differ in parameter type, number, or order (resolved at compile-time / static binding). Method Overriding occurs between a superclass and subclass where the subclass provides a specific implementation of a method already defined in the superclass with identical signature and return type (resolved at runtime / dynamic binding).",
    explanation: "Overloading does not depend on inheritance; overriding requires inheritance and runtime dynamic dispatch.",
    betterAnswer: "Method Overloading is compile-time (static) polymorphism in the same class where methods share the same name with different parameter signatures (types, count, or sequence); return type alone cannot differentiate overloaded methods. Method Overriding is runtime (dynamic) polymorphism where a subclass redefines an inherited method using the exact same signature and compatible (covariant) return type, annotated with `@Override`. Method overriding is resolved via dynamic dispatch (vtable) based on the actual runtime object instance.",
    difficulty: "Easy",
    marks: 2,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "OOP Concepts"
  },
  {
    questionId: "benchmark-tech-018",
    question: "Explain the difference between an Abstract Class and an Interface in Java/C++.",
    expectedAnswer: "An Abstract Class can have state (instance variables), constructors, and both abstract and concrete methods with full implementations; a class can only inherit from one abstract class (single inheritance). An Interface defines a pure contract with method signatures (though modern Java supports default/static methods), cannot maintain instance state, and a class can implement multiple interfaces (multiple inheritance).",
    explanation: "Abstract classes represent 'is-a' identity hierarchies, while interfaces represent 'can-do' capabilities across unrelated classes.",
    betterAnswer: "Key differences: (1) Multiple Inheritance: A class can implement multiple interfaces but extend only one abstract class. (2) State & Fields: Abstract classes can declare non-static, mutable instance variables and constructors; interfaces only allow `public static final` constants. (3) Methods: Abstract classes contain both fully implemented concrete methods and abstract declarations; interfaces originally contained only abstract methods, though Java 8+ added default and static methods. (4) Conceptual Purpose: Abstract classes share code and identity among closely related subclasses ('is-a'), while interfaces define decoupled capability contracts ('can-do') across unrelated type hierarchies.",
    difficulty: "Medium",
    marks: 3,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "OOP Concepts"
  },
  {
    questionId: "benchmark-tech-019",
    question: "What is the Diamond Problem in multiple inheritance, and how do languages like Java or C++ address it?",
    expectedAnswer: "The Diamond Problem occurs when a class D inherits from two classes B and C, which both inherit from a common base class A. If B and C override a method from A, class D faces ambiguity on which version to inherit. C++ resolves this using virtual inheritance (`virtual public A`). Java prevents it by disallowing multiple class inheritance and requiring explicit override resolution for default interface method collisions.",
    explanation: "The inheritance graph forms a diamond shape (A at top, B and C in middle, D at bottom), creating duplicate base subobjects.",
    betterAnswer: "The Diamond Problem arises in multiple inheritance when class D inherits from two classes B and C that share a common ancestor A. If A defines `foo()` and both B and C override it, calling `d.foo()` creates ambiguity regarding whether B's or C's implementation should execute. C++ solves this using `virtual` base classes (`class B : virtual public A`), ensuring only a single instance of A exists in D. Java disallows multiple class inheritance entirely; for interface default method conflicts, Java requires the implementing class to explicitly override the conflicting method and specify `InterfaceName.super.foo()`.",
    difficulty: "Hard",
    marks: 5,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "practice",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "OOP Concepts"
  },
  {
    questionId: "benchmark-tech-020",
    question: "Explain the SOLID design principles in Object-Oriented software engineering.",
    expectedAnswer: "SOLID stands for: Single Responsibility Principle (a class should have only one reason to change), Open/Closed Principle (software entities should be open for extension but closed for modification), Liskov Substitution Principle (subclasses must be substitutable for their base classes), Interface Segregation Principle (clients should not be forced to depend on interfaces they do not use), and Dependency Inversion Principle (depend on abstractions, not concrete implementations).",
    explanation: "SOLID principles prevent code rot, tight coupling, and rigidity in enterprise software systems.",
    betterAnswer: "SOLID principles ensure maintainable, decoupled object-oriented architecture: (1) SRP (Single Responsibility): A module/class should encapsulate a single responsibility or business actor. (2) OCP (Open/Closed): Classes should allow behavior extension (via inheritance/polymorphism/composition) without modifying existing source code. (3) LSP (Liskov Substitution): Derived types must be completely substitutable for their base types without breaking program correctness. (4) ISP (Interface Segregation): Prefer fine-grained, role-specific interfaces over bloated 'fat' interfaces. (5) DIP (Dependency Inversion): High-level modules should not depend on low-level modules; both must depend on abstractions (interfaces).",
    difficulty: "Hard",
    marks: 5,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "OOP Concepts"
  },
  {
    questionId: "benchmark-tech-021",
    question: "What is the difference between Composition and Inheritance? Why is 'Composition over Inheritance' recommended?",
    expectedAnswer: "Inheritance represents an 'is-a' relationship where a subclass tightly couples to a superclass implementation. Composition represents a 'has-a' relationship where a class contains instances of other classes and delegates tasks to them. 'Composition over Inheritance' is recommended because it provides loose coupling, allows changing behavior at runtime, and prevents fragile base class issues.",
    explanation: "Inheritance exposes superclass internal implementation details to subclasses (white-box reuse), whereas composition uses interfaces (black-box reuse).",
    betterAnswer: "Inheritance establishes tight compile-time coupling ('is-a') where changes in the parent class can inadvertently break derived class invariants (the fragile base class problem). Composition establishes flexible runtime relationships ('has-a') where a class references other objects as components and invokes their methods. 'Favor Composition over Inheritance' is recommended because: (1) Components can be swapped dynamically at runtime (e.g., via Strategy Pattern or Dependency Injection), (2) It adheres to the Single Responsibility Principle, and (3) It encapsulates component internals behind stable interfaces.",
    difficulty: "Medium",
    marks: 3,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "practice",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "OOP Concepts"
  },
  {
    questionId: "benchmark-tech-022",
    question: "Explain the Singleton design pattern and describe how to make it thread-safe.",
    expectedAnswer: "The Singleton pattern ensures a class has only one instance and provides a global access point to it. It has a private constructor, a private static instance variable, and a public static `getInstance()` method. It is made thread-safe using Double-Checked Locking with a `volatile` instance variable or using the Bill Pugh Static Holder idiom / Java Enum.",
    explanation: "Without synchronization, concurrent threads entering `getInstance()` simultaneously can instantiate multiple objects.",
    betterAnswer: "The Singleton design pattern restricts instantiation to a single object per JVM. Thread-safe implementation strategies include: (1) Double-Checked Locking: Declare `private static volatile Singleton instance;` and inside `getInstance()`, check `if (instance == null)` before synchronizing on the class and checking `instance == null` again before instantiation (the `volatile` keyword prevents instruction reordering). (2) Bill Pugh Inner Static Holder: Utilizes JVM class-loader thread safety where an inner static class loads only when `getInstance()` is called. (3) Enum Singleton: Enums guarantee thread-safe, reflection-proof single instance creation automatically.",
    difficulty: "Medium",
    marks: 3,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Design Patterns"
  },

  // ── 3. Database / SQL & DBMS (Q23 - Q65) ──
  {
    questionId: "benchmark-tech-023",
    question: "Explain the ACID properties in Relational Database Management Systems (RDBMS).",
    expectedAnswer: "ACID stands for: Atomicity (all operations in a transaction succeed, or none are applied), Consistency (database transitions from one valid state to another, satisfying all constraints), Isolation (concurrent transactions execute independently without interference), and Durability (committed changes survive system crashes and power failures via WAL / transaction logs).",
    explanation: "ACID guarantees transactional integrity and data reliability in multi-user database systems.",
    betterAnswer: "ACID properties define database transaction reliability: (1) Atomicity: 'All-or-nothing' execution where failure of any statement triggers a complete rollback (managed via rollback undo logs). (2) Consistency: Guarantees that transactions preserve all schema rules, constraints, triggers, and foreign keys. (3) Isolation: Controls concurrent transaction visibility through isolation levels (Read Uncommitted, Read Committed, Repeatable Read, Serializable) using locks or MVCC. (4) Durability: Once a transaction commits, its modifications are permanently recorded in non-volatile storage using Write-Ahead Logging (WAL).",
    difficulty: "Easy",
    marks: 2,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Database & SQL"
  },
  {
    questionId: "benchmark-tech-024",
    question: "What is the difference between WHERE and HAVING clauses in SQL?",
    expectedAnswer: "The WHERE clause filters individual rows before any aggregation takes place and cannot be used with aggregate functions (like SUM, AVG, COUNT). The HAVING clause filters grouped rows after the GROUP BY clause and is used specifically to filter on aggregate function results.",
    explanation: "SQL logical query execution evaluates WHERE before GROUP BY, and HAVING after GROUP BY.",
    betterAnswer: "In SQL query execution order, `WHERE` is evaluated early (after `FROM` and `JOIN`) to filter base table rows prior to grouping; it cannot contain aggregate functions because aggregation has not yet occurred. `HAVING` is evaluated after `GROUP BY` and `AGGREGATION` to filter grouped summary rows based on aggregate expressions (e.g. `HAVING COUNT(*) > 5`). For optimal performance, filter raw non-aggregate predicates in `WHERE` to minimize rows processed by `GROUP BY`.",
    difficulty: "Easy",
    marks: 2,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Database & SQL"
  },
  {
    questionId: "benchmark-tech-025",
    question: "Explain the differences between INNER JOIN, LEFT JOIN, RIGHT JOIN, and FULL OUTER JOIN.",
    expectedAnswer: "INNER JOIN returns only rows with matching keys in both tables. LEFT JOIN returns all rows from the left table and matched rows from the right table (filling NULLs for non-matches). RIGHT JOIN returns all rows from the right table and matched rows from the left table. FULL OUTER JOIN returns all rows when there is a match in either table, filling NULLs where no match exists.",
    explanation: "Joins combine columns from one or more tables based on related key columns.",
    betterAnswer: "Relational SQL Joins: (1) INNER JOIN: Returns only tuples satisfying the join predicate in both tables (intersection). (2) LEFT (OUTER) JOIN: Returns all records from the left table; matching right table records are appended, and unmatched right columns are populated with NULL. (3) RIGHT (OUTER) JOIN: Returns all records from the right table along with matched left table records (NULL for unmatched left attributes). (4) FULL OUTER JOIN: Combines results of both LEFT and RIGHT joins (union), returning all rows from both tables and substituting NULL wherever join predicates are unsatisfied.",
    difficulty: "Easy",
    marks: 2,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Database & SQL"
  },
  {
    questionId: "benchmark-tech-026",
    question: "Write an SQL query to find the second highest salary from an Employee table without using vendor-specific limits.",
    expectedAnswer: "SELECT MAX(salary) FROM Employee WHERE salary < (SELECT MAX(salary) FROM Employee); Or using DENSE_RANK(): SELECT salary FROM (SELECT salary, DENSE_RANK() OVER (ORDER BY salary DESC) as rnk FROM Employee) t WHERE rnk = 2;",
    explanation: "The subquery finds the absolute maximum salary, and the outer query finds the maximum among salaries strictly less than that maximum.",
    betterAnswer: "Standard ANSI-SQL solutions: (1) Correlated / Subquery approach: `SELECT MAX(salary) AS SecondHighestSalary FROM Employee WHERE salary < (SELECT MAX(salary) FROM Employee);` (2) Window function approach (handles duplicate salaries cleanly): `SELECT salary FROM (SELECT salary, DENSE_RANK() OVER (ORDER BY salary DESC) AS ranking FROM Employee) AS ranked WHERE ranking = 2 LIMIT 1;` Using DENSE_RANK ensures that if two employees share the top salary, the next distinct salary is correctly identified as the 2nd highest.",
    difficulty: "Medium",
    marks: 3,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Database & SQL"
  },
  {
    questionId: "benchmark-tech-027",
    question: "What is Database Normalization? Explain 1NF, 2NF, and 3NF.",
    expectedAnswer: "Normalization is the process of organizing database tables to minimize data redundancy and eliminate insert, update, and delete anomalies. 1NF requires atomic (indivisible) column values and unique rows. 2NF is in 1NF and removes partial dependencies (non-key attributes must depend on the whole primary key). 3NF is in 2NF and removes transitive dependencies (non-key attributes must not depend on other non-key attributes).",
    explanation: "Normalization decomposes relations based on functional dependencies to achieve data integrity.",
    betterAnswer: "Database Normalization systematically decomposes tables to eliminate data redundancy and insertion/deletion/update anomalies: (1) 1NF (First Normal Form): Eliminates repeating groups, ensuring each column holds atomic, scalar values and each record has a primary key. (2) 2NF (Second Normal Form): Meets 1NF and eliminates Partial Functional Dependencies — all non-prime attributes must be fully functionally dependent on the entire primary key (relevant for composite keys). (3) 3NF (Third Normal Form): Meets 2NF and eliminates Transitive Dependencies — non-prime attributes must depend directly only on candidate keys (X -> Y where Y is non-prime implies X is a superkey).",
    difficulty: "Medium",
    marks: 3,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Database & SQL"
  },
  {
    questionId: "benchmark-tech-028",
    question: "What is an Index in a database, how does a B-Tree index work, and what are the trade-offs of creating too many indexes?",
    expectedAnswer: "A database index is a data structure (commonly a B-Tree or B+Tree) that accelerates data retrieval queries without scanning entire tables. A B-Tree maintains sorted keys in self-balancing nodes, allowing O(log n) lookups and range scans. The trade-offs of having too many indexes include increased storage consumption and slower INSERT, UPDATE, and DELETE operations because all related indexes must be updated on every write.",
    explanation: "Indexes speed up read operations (SELECT) at the cost of additional write overhead (DML) and disk space.",
    betterAnswer: "An Index is an auxiliary search structure (typically B+Tree in relational engines like PostgreSQL/MySQL) where internal nodes store routing keys and leaf nodes store sorted keys with row pointers/tuples, enabling O(log n) point lookups and sequential range scans. Trade-offs of excessive indexing: (1) Write Penalties: Every `INSERT`, `UPDATE`, and `DELETE` must re-balance and update all associated index trees, degrading DML throughput. (2) Disk/Memory Overhead: Indexes consume significant RAM and storage. (3) Optimizer Confusion: Too many indexes can increase query planner latency.",
    difficulty: "Medium",
    marks: 3,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Database & SQL"
  },
  {
    questionId: "benchmark-tech-029",
    question: "Explain the difference between Primary Key, Unique Key, and Foreign Key constraints.",
    expectedAnswer: "A Primary Key uniquely identifies each record in a table, cannot contain NULL values, and a table can have only one Primary Key. A Unique Key enforces uniqueness of values across rows but permits one or more NULL values (depending on SQL dialect), and multiple Unique Keys can exist per table. A Foreign Key establishes a referential relationship to a Primary or Unique Key in another table, enforcing referential integrity.",
    explanation: "Primary and Unique keys enforce entity integrity; Foreign keys enforce referential integrity between tables.",
    betterAnswer: "Key constraints: (1) Primary Key: Enforces entity integrity, ensuring unique identification of every row. It strictly disallows `NULL` values and creates a default clustered index in many engines (only 1 per table). (2) Unique Key: Enforces uniqueness across columns but allows `NULL` values (standard SQL permits multiple NULLs since NULL != NULL). A table can declare multiple unique constraints. (3) Foreign Key: Enforces referential integrity between two tables by ensuring the value matches an existing Primary/Unique key in the referenced parent table or is NULL, preventing orphaned records and supporting cascading updates/deletions.",
    difficulty: "Easy",
    marks: 2,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Database & SQL"
  },
  {
    questionId: "benchmark-tech-030",
    question: "Explain the difference between TRUNCATE, DROP, and DELETE statements in SQL.",
    expectedAnswer: "DELETE is a DML command that removes specific rows matching a WHERE clause, logs each row deletion in the transaction log, triggers database triggers, and can be rolled back. TRUNCATE is a DDL command that deallocates entire data pages, removing all rows rapidly with minimal logging without firing triggers. DROP is a DDL command that completely deletes both the table structure and its data from the database catalog.",
    explanation: "DELETE works row-by-row; TRUNCATE deallocates pages; DROP deletes the table definition entirely.",
    betterAnswer: "Comparison: (1) DELETE: DML command. Deletes specific or all rows using optional `WHERE` clause. Generates undo logs for each row, activates `DELETE` triggers, updates indexes row-by-row, and supports complete transactional rollback. (2) TRUNCATE: DDL command. Fast table wipe that deallocates the underlying data pages directly rather than deleting rows individually. Resets identity/auto-increment counters, does not fire row triggers, and is minimally logged. (3) DROP: DDL command. Completely removes table schema definition, metadata, indexes, permissions, and data from the database catalog.",
    difficulty: "Easy",
    marks: 2,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "practice",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Database & SQL"
  },

  // ── 4. Programming Languages (Java, C++, Python, PHP) (Q31 - Q55) ──
  {
    questionId: "benchmark-tech-031",
    question: "Explain how Garbage Collection works in Java and what the terms Young, Old, and Metaspace refer to.",
    expectedAnswer: "Java Garbage Collection automatically reclaims heap memory occupied by unreferenced objects using generational collection. The Young Generation holds newly allocated objects (Eden and Survivor spaces S0/S1) where minor GC occurs frequently. Objects surviving several cycles are promoted to the Old (Tenured) Generation where Major/Full GC occurs. Metaspace (in native memory) stores class metadata, bytecodes, and method definitions.",
    explanation: "The Generational Hypothesis states that most objects die young, making frequent minor collections fast and efficient.",
    betterAnswer: "Java Garbage Collection utilizes generational memory management: (1) Young Generation: Newly instantiated objects reside in Eden. When Eden fills, a Minor GC moves surviving objects to Survivor space (S0 or S1). (2) Aging & Promotion: Objects that survive multiple garbage collection thresholds (tenuring threshold) are promoted to the Old (Tenured) Generation. (3) Old Generation: Holds long-lived objects; reclaimed via Major/Full GC algorithms (e.g. G1GC, ZGC) using mark-sweep-compact phases. (4) Metaspace: Replaced PermGen in Java 8; resides in off-heap native memory to store loaded class metadata, avoiding PermGen OutOfMemoryErrors.",
    difficulty: "Medium",
    marks: 3,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Java Programming"
  },
  {
    questionId: "benchmark-tech-032",
    question: "Why are Strings immutable in Java and what are the benefits of string immutability?",
    expectedAnswer: "Strings are immutable in Java (their character sequence cannot be modified after creation) for security, caching, and thread-safety reasons. Benefits include: (1) String Pool caching reduces memory overhead, (2) Thread-safety without synchronization, (3) Safe caching of hash codes (making HashMaps fast and reliable), and (4) Security when passing strings as network URLs, file paths, and database credentials.",
    explanation: "The String class is final and its internal byte/char array is private and final without mutator methods.",
    betterAnswer: "Strings are immutable in Java for four primary design reasons: (1) String Constant Pool Optimization: Reusing identical string literals in the intern pool saves substantial heap space. (2) Thread Safety: Immutable objects are inherently thread-safe and can be shared freely across concurrent threads without locks. (3) Security: Strings represent sensitive parameters (database connection URLs, classloader paths, passwords); immutability prevents malicious tampering after validation. (4) Hash Code Caching: The hash code is computed lazily and cached permanently, ensuring consistent O(1) performance in `HashMap` and `HashSet`.",
    difficulty: "Easy",
    marks: 2,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Java Programming"
  },
  {
    questionId: "benchmark-tech-033",
    question: "Explain the difference between `==` and `.equals()` method in Java and C#.",
    expectedAnswer: "In Java, `==` is a reference equality operator that checks whether two object references point to the exact same memory address on the heap (or compares primitive values directly). The `.equals()` method is an overridable method that evaluates semantic/content equality between two objects (e.g. comparing the characters of two distinct String instances).",
    explanation: "Object class default `.equals()` uses `==`, but classes like String, Integer, and Date override it to compare contents.",
    betterAnswer: "In Java: `==` evaluates referential identity for objects (verifying if referenceA and referenceB hold the identical heap memory address) and value equality for primitive types (`int`, `boolean`). The `.equals()` method defined in `java.lang.Object` defaults to `==`, but standard classes (like `String`, `List`, `Map`, wrapper types) override `.equals()` to perform deep structural and value equivalence. Whenever overriding `.equals()`, the `hashCode()` contract must also be preserved.",
    difficulty: "Easy",
    marks: 2,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Java Programming"
  },
  {
    questionId: "benchmark-tech-034",
    question: "What is a Pointer in C/C++, and what is the difference between a Pointer and a Reference?",
    expectedAnswer: "A pointer is a variable that stores the memory address of another variable, can be reassigned to point elsewhere, and can hold NULL. A reference is an alias to an existing variable, must be initialized upon declaration, cannot be NULL, and cannot be reseated to refer to another object.",
    explanation: "Pointers allow arithmetic (`ptr++`) and explicit dereferencing (`*ptr`), whereas references syntax matches regular variables.",
    betterAnswer: "In C/C++: (1) Pointer (`T*`): A distinct variable storing a memory address. Can be initialized to `nullptr`/`NULL`, reassigned to point to different memory addresses throughout its lifetime, supports pointer arithmetic, and requires explicit dereferencing (`*p`, `->`). (2) Reference (`T&`): An immutable syntax alias to an already existing object. Must be bound at definition, cannot be `null`, cannot be reseated to another variable, does not support pointer arithmetic, and accesses members directly with the dot operator (`.`).",
    difficulty: "Easy",
    marks: 2,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "practice",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "C/C++ Programming"
  },
  {
    questionId: "benchmark-tech-035",
    question: "Explain Memory Leaks in C++ and how Smart Pointers (`std::unique_ptr`, `std::shared_ptr`, `std::weak_ptr`) prevent them.",
    expectedAnswer: "A memory leak occurs when dynamically allocated heap memory (via `new` or `malloc`) is no longer referenced but is never freed (via `delete` or `free`). Smart pointers in modern C++ use RAII (Resource Acquisition Is Initialization) to automatically manage memory: `std::unique_ptr` provides exclusive ownership and auto-deletes on scope exit; `std::shared_ptr` uses reference counting to free memory when the last owner is destroyed; `std::weak_ptr` prevents cyclic reference leaks.",
    explanation: "Smart pointer destructors automatically invoke delete when control leaves the containing scope.",
    betterAnswer: "Memory leaks occur in C++ when heap memory allocated via `new` is orphaned without calling `delete`, wasting system RAM. Modern C++ (C++11+) solves this via RAII smart pointers: (1) `std::unique_ptr`: Enforces exclusive single-ownership with zero runtime overhead; automatically deallocates the resource when going out of scope; non-copyable but movable. (2) `std::shared_ptr`: Implements shared ownership via an atomic reference count control block; deletes the managed object when reference count reaches zero. (3) `std::weak_ptr`: Holds non-owning references to `shared_ptr` objects without incrementing reference counts, breaking circular reference deadlocks.",
    difficulty: "Medium",
    marks: 3,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "practice",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "C/C++ Programming"
  },
  {
    questionId: "benchmark-tech-036",
    question: "Explain the Global Interpreter Lock (GIL) in Python and how it affects multi-threaded CPU-bound programs.",
    expectedAnswer: "The GIL (Global Interpreter Lock) in CPython is a mutex that allows only one native thread to execute Python bytecode at a time, even on multi-core processors. Because of the GIL, multi-threading in Python does not speed up CPU-bound tasks (which are better handled using the `multiprocessing` module), but it remains effective for I/O-bound tasks (network/disk operations).",
    explanation: "The GIL simplifies memory management by ensuring thread-safe reference counting in CPython.",
    betterAnswer: "The Global Interpreter Lock (GIL) is a synchronization mechanism in CPython designed to protect internal memory management and reference counting from race conditions. It restricts bytecode execution to a single thread at any instant. Consequently, multi-threaded Python programs cannot achieve true CPU parallel processing across multiple CPU cores for compute-heavy tasks. For CPU-bound workloads, developers use `multiprocessing` (spawning separate OS processes with distinct Python interpreters) or C extensions, whereas multi-threading or `asyncio` is optimal for I/O-bound concurrency.",
    difficulty: "Medium",
    marks: 3,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "practice",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Python Programming"
  },

  // ── 5. Web, REST APIs & Frameworks (Q37 - Q55) ──
  {
    questionId: "benchmark-tech-037",
    question: "Explain the architectural constraints and principles of a RESTful API.",
    expectedAnswer: "A RESTful API follows 6 architectural constraints: (1) Client-Server separation, (2) Statelessness (each request contains all context needed to process it), (3) Cacheability (responses explicitly declare if they are cacheable), (4) Uniform Interface (standard HTTP verbs, URI resource naming, JSON representations), (5) Layered System (clients cannot tell if they connect directly or via proxies), and (6) Code on Demand (optional).",
    explanation: "REST (Representational State Transfer) leverages existing HTTP infrastructure for scalable, decoupled web services.",
    betterAnswer: "RESTful architecture defines 6 core constraints: (1) Statelessness: Server retains no client session context; each request carries complete authentication/payload data. (2) Uniform Interface: Identifies resources via clean URIs (e.g., `/api/v1/users`), manipulates resources through standard HTTP verbs (`GET`, `POST`, `PUT`, `PATCH`, `DELETE`), uses standardized media types (JSON), and supports hypermedia (HATEOAS). (3) Client-Server Decoupling: Separates user interface concerns from data storage concerns. (4) Cacheability: HTTP headers (`Cache-Control`, `ETag`) define response freshness. (5) Layered Architecture: Intermediaries (load balancers, gateways, caches) operate transparently to clients.",
    difficulty: "Medium",
    marks: 3,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Web & REST APIs"
  },
  {
    questionId: "benchmark-tech-038",
    question: "What is the difference between PUT and PATCH HTTP methods?",
    expectedAnswer: "PUT replaces the target resource entirely with the request payload (idempotent; missing fields in payload are overwritten or set to default). PATCH applies partial modifications to the target resource, updating only the specific fields included in the request payload (not necessarily idempotent).",
    explanation: "PUT is a full resource replacement; PATCH is a delta/partial update.",
    betterAnswer: "HTTP Method semantics: (1) `PUT`: Full resource replacement. The client sends a complete representation of the entity. Any unsupplied attributes are overwritten or nullified according to schema rules. `PUT` is strictly idempotent (multiple identical `PUT` requests produce the identical resource state). (2) `PATCH`: Partial resource update. The client sends only the subset of fields to be modified (delta updates). While often idempotent in practice, RFC 5789 specifies that `PATCH` is not inherently idempotent.",
    difficulty: "Easy",
    marks: 2,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "Web & REST APIs"
  },
  {
    questionId: "benchmark-tech-039",
    question: "Explain the JavaScript Event Loop, Call Stack, Task Queue (Macrotasks), and Microtask Queue.",
    expectedAnswer: "JavaScript is single-threaded and executes synchronous code on the Call Stack. When asynchronous operations complete (e.g. fetch, setTimeout), their callbacks enter queues. Promises and MutationObserver callbacks go to the Microtask Queue, while setTimeout/setInterval go to the Task Queue (Macrotask). The Event Loop continually checks: whenever the Call Stack is empty, it processes ALL Microtasks first before picking the next Macrotask.",
    explanation: "Microtasks have strict priority over Macrotasks in the browser/Node.js event loop cycle.",
    betterAnswer: "The JavaScript runtime concurrency model consists of: (1) Call Stack: Single-threaded LIFO execution stack for synchronous frames. (2) Web APIs / Node APIs: Handle background async operations (timers, network I/O). (3) Microtask Queue: Highest priority queue for Promise callbacks (`.then()`, `async/await`), `process.nextTick()`, and `queueMicrotask()`. (4) Macrotask Queue (Task Queue): Holds `setTimeout`, `setInterval`, `setImmediate`, and UI events. (5) Event Loop: Once the Call Stack is completely clear, it empties the entire Microtask Queue before executing a single Macrotask, repeating this cycle continuously.",
    difficulty: "Hard",
    marks: 5,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "interview_reported",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "JavaScript & Web"
  },
  {
    questionId: "benchmark-tech-040",
    question: "Explain the difference between Props and State in React components.",
    expectedAnswer: "Props (short for properties) are read-only inputs passed from a parent component to a child component to configure it (immutable from child's perspective). State is internal data managed within the component itself that can change over time (using `useState`), where state changes trigger component re-rendering.",
    explanation: "Props flow downwards (unidirectional data flow), while state is private to the component declaring it.",
    betterAnswer: "In React: (1) `Props`: Immutable external parameters passed from parent to child components, establishing predictable top-down data flow. A child component must treat its props as pure, read-only inputs. (2) `State`: An encapsulated, mutable data store managed internally by a component (via `useState` or `useReducer`). When state updates are scheduled, React queues a re-render to reconcile the Virtual DOM and update the UI.",
    difficulty: "Easy",
    marks: 2,
    questionType: "Technical",
    options: [],
    correctAnswer: "",
    source: "practice",
    companyId: "benchmark",
    companyIds: ["benchmark"],
    topic: "React & Frontend"
  }
];

// Fill out remaining programmatically generated comprehensive questions (up to 160)
// spanning DSA, OOP, SQL, Programming, Web, and CS Fundamentals.
const topicsList = [
  { topic: "Sorting & Algorithms", diff: "Easy", marks: 2, q: "Explain the Bubble Sort algorithm and its best-case optimization using a boolean swapped flag.", a: "Bubble sort repeatedly swaps adjacent elements if out of order. With a boolean swapped flag, if no swaps occur in a pass, the array is already sorted, reducing best-case time from O(n^2) to O(n).", exp: "The swapped flag detects sorted input on the very first pass.", bet: "Standard Bubble Sort takes O(n^2) in all cases. By tracking whether any swap occurred during an inner loop pass with a boolean flag, the algorithm terminates early if the array is already sorted, achieving O(n) best-case time with O(1) space." },
  { topic: "Sorting & Algorithms", diff: "Easy", marks: 2, q: "Explain the working of Insertion Sort and why it is efficient for small or nearly-sorted datasets.", a: "Insertion sort builds the sorted array one element at a time by inserting each element into its proper position among previously sorted elements. It runs in O(n) time on nearly-sorted data and has low constant overhead.", exp: "Elements only shift as far as necessary rather than traversing the entire array.", bet: "Insertion Sort iterates through an array, taking the current element and shifting greater elements rightward until its correct sorted slot is found. For small arrays (n < 15) or nearly sorted sequences, it exhibits linear O(n) runtime and minimal overhead, which is why TimSort and QuickSort use it as a sub-routine." },
  { topic: "Sorting & Algorithms", diff: "Medium", marks: 3, q: "What is Radix Sort and how does it achieve linear time complexity O(d * (n + k))?", a: "Radix Sort is a non-comparative sorting algorithm that sorts numbers digit-by-digit from least significant to most significant digit using a stable subroutine like Counting Sort. Since it avoids comparisons, it runs in O(d * (n + k)) time.", exp: "By processing digits sequentially, it avoids the O(n log n) comparison lower bound.", bet: "Radix Sort processes keys digit by digit (or byte by byte) from least significant (LSD) to most significant (MSD) using a stable intermediate sort (Counting Sort). Because it does not perform pairwise element comparisons, it bypasses the Omega(n log n) lower bound, sorting n d-digit keys in O(d * (n + k)) time and O(n + k) auxiliary space." },
  { topic: "Searching & Algorithms", diff: "Medium", marks: 3, q: "Explain how Breadth-First Search (BFS) and Depth-First Search (DFS) differ in graph traversal.", a: "BFS explores neighbor nodes layer by layer using a FIFO queue and finds shortest paths in unweighted graphs. DFS explores as deep as possible along each branch before backtracking using a LIFO stack or recursion.", exp: "BFS is breadth-oriented using O(V) queue space; DFS is depth-oriented using recursion call stack.", bet: "BFS explores vertices in concentric distance tiers using a FIFO queue, guaranteeing shortest path discovery in unweighted graphs with O(V + E) time and O(V) space. DFS explores paths to maximum depth before backtracking using a LIFO stack/recursion, useful for topological sorting, cycle detection, and connected components." },
  { topic: "Data Structures", diff: "Medium", marks: 3, q: "What is a Trie (Prefix Tree) and what are its primary use cases in real-world software?", a: "A Trie is a tree data structure used to store a dynamic set of strings where keys are usually strings. Nodes share common prefixes. Primary use cases include autocomplete/search suggestions, spell checkers, and IP routing tables.", exp: "Lookup and insert take O(L) time where L is string length, independent of the number of words stored.", bet: "A Trie (Prefix Tree) is an m-ary tree where each node represents a character, and descendants share common string prefixes. It enables O(L) prefix search, insertion, and auto-completion (where L is query length) regardless of dictionary size N, making it ideal for search engine typeahead suggestions, IP routing lookup, and spell checking." },
  { topic: "Data Structures", diff: "Hard", marks: 5, q: "Explain the structure and operational complexity of a Disjoint Set Union (DSU / Union-Find) with path compression and union by rank.", a: "DSU maintains a collection of disjoint sets. With path compression (flattening tree during find) and union by rank (attaching smaller tree under larger root), operations run in nearly constant amortized time O(alpha(n)), where alpha is the Inverse Ackermann function.", exp: "Both optimizations together ensure tree height remains practically <= 4 for all universe sizes.", bet: "Disjoint Set Union (Union-Find) manages partition sets supporting `find(x)` (locate representative root) and `union(x, y)` (merge two sets). Union by Rank attaches the shallower tree under the deeper tree root, bounding height to O(log n). Path Compression flattens the node tree during `find()`, pointing all traversed nodes directly to root. Combined, any sequence of m operations takes O(m * alpha(n)) time, where alpha is the extremely slow-growing Inverse Ackermann function." },
  { topic: "Data Structures", diff: "Easy", marks: 2, q: "What is a Circular Queue and how does it solve the memory limitation of a standard linear queue array?", a: "A Circular Queue connects the last position back to the first position using modulo arithmetic `(rear + 1) % size`. This allows reusing empty spaces created by dequeued elements without shifting remaining items.", exp: "Modulo arithmetic creates a continuous ring buffer.", bet: "In a standard array-based queue, dequeuing elements leaves empty indices at the front that cannot be reused without expensive O(n) shifting. A Circular Queue wraps the rear and front pointers around to index 0 using modulo arithmetic `index = (index + 1) % capacity`, enabling full utilization of allocated memory in O(1) time per enqueue/dequeue." },
  { topic: "Database & SQL", diff: "Easy", marks: 2, q: "Explain the purpose of Foreign Key constraints and cascading actions (ON DELETE CASCADE).", a: "Foreign keys enforce referential integrity between parent and child tables. ON DELETE CASCADE specifies that when a row in the parent table is deleted, all matching dependent rows in the child table are automatically deleted.", exp: "Prevents orphaned records in child tables.", bet: "A Foreign Key establishes a relational constraint between a child column and parent primary key. `ON DELETE CASCADE` instructs the database engine to automatically remove all referencing child records whenever their parent record is deleted, maintaining referential integrity without manual child-row cleanup." },
  { topic: "Database & SQL", diff: "Medium", marks: 3, q: "What are Database Views and what is the difference between a Standard View and a Materialized View?", a: "A standard view is a virtual table defined by an SQL query whose data is dynamically generated on each query. A Materialized View physically stores the precomputed query results on disk and must be refreshed periodically, accelerating heavy aggregation queries.", exp: "Standard views do not consume storage; materialized views trade disk storage and refresh overhead for fast reads.", bet: "A Standard View is a saved SQL query definition acting as a virtual table; executing `SELECT * FROM my_view` re-runs the underlying query at runtime without consuming disk space. A Materialized View physically caches and persists the query result set to disk, enabling instant O(1)/indexed reads on complex analytical queries, requiring scheduled refreshes (`REFRESH MATERIALIZED VIEW`) to sync updates." },
  { topic: "Database & SQL", diff: "Hard", marks: 5, q: "Explain the four ANSI SQL Transaction Isolation Levels and the concurrency anomalies they prevent (Dirty Read, Non-Repeatable Read, Phantom Read).", a: "The four isolation levels are: (1) Read Uncommitted (allows dirty reads), (2) Read Committed (prevents dirty reads), (3) Repeatable Read (prevents dirty and non-repeatable reads), and (4) Serializable (prevents all anomalies including phantom reads using strict locking or serial ordering).", exp: "Higher isolation levels increase consistency but reduce concurrent transaction throughput.",
    bet: "Transaction Isolation Levels: (1) Read Uncommitted: Allows Dirty Reads (reading uncommitted changes from concurrent transactions). (2) Read Committed: Prevents Dirty Reads by ensuring queries only read committed data; permits Non-Repeatable Reads (row values change between reads). (3) Repeatable Read: Prevents Dirty and Non-Repeatable Reads via snapshot reads/shared locks; may allow Phantom Reads (new rows inserted by another transaction). (4) Serializable: Highest isolation; prevents all anomalies using two-phase locking (2PL) or SSI, ensuring transactions execute as if strictly serial." },
  { topic: "Database & SQL", diff: "Medium", marks: 3, q: "What is Database Sharding vs Database Partitioning?", a: "Partitioning splits a large table into smaller segments within a single database instance (e.g. range, list, hash partitioning). Sharding is horizontal partitioning across multiple independent physical database servers to distribute load and storage.", exp: "Partitioning scales vertically within one node; sharding scales horizontally across a distributed cluster.", bet: "Partitioning divides tables into logical subsets (by date range, hash, or list) residing on the same database server, improving query pruning and index maintenance. Sharding is horizontal partitioning across multiple distinct physical servers/nodes with shared-nothing architecture, where a routing key directs queries to specific shards, scaling storage and write throughput horizontally." },
  { topic: "OOP Concepts", diff: "Easy", marks: 2, q: "What is the difference between a Constructor and a Normal Method in OOP?", a: "A constructor is a special member function with the same name as the class and no return type, automatically invoked when an object is instantiated to initialize state. A normal method has an explicit return type and must be called explicitly on an object.", exp: "Constructors initialize memory; methods define ongoing behaviors.", bet: "A Constructor is a special initialization method sharing the class name with no return type; it executes automatically upon `new` instantiation to establish valid object state. A regular method defines object behaviors, declares an explicit return type (or `void`), and must be invoked explicitly on existing instances." },
  { topic: "OOP Concepts", diff: "Medium", marks: 3, q: "What are Access Modifiers in Java/C++ (`public`, `protected`, `private`, default/package-private)?", a: "Public members are accessible from anywhere. Protected members are accessible within the same package and subclasses. Default (package-private) members are accessible only within the same package. Private members are accessible only within the declaring class.", exp: "Access modifiers enforce encapsulation by scoping variable and method visibility.", bet: "Access modifiers control visibility: (1) `public`: Accessible across all packages and classes. (2) `protected`: Accessible within the same package and by subclasses in other packages. (3) Default (no modifier in Java): Package-private, accessible only within the same package. (4) `private`: Accessible strictly within the declaring class, preventing direct external mutation." },
  { topic: "OOP Concepts", diff: "Hard", marks: 5, q: "Explain the Factory Pattern vs Abstract Factory Pattern with practical software examples.", a: "Factory Method defines an interface for creating a single object, letting subclasses decide which class to instantiate. Abstract Factory provides an interface for creating families of related or dependent objects without specifying their concrete classes (e.g., creating UI components for Windows vs Mac).", exp: "Factory creates one product; Abstract Factory creates families of related products.", bet: "Factory Method uses inheritance and polymorphic methods to instantiate a single product type (e.g., `NotificationFactory.createNotification('SMS')`). Abstract Factory provides a top-level factory interface composing multiple factory methods to produce entire families of related/dependent products without coupling to concrete classes (e.g., `GUIFactory` producing `Button`, `Checkbox`, `Scrollbar` configured for either `WindowsGUI` or `MacOSGUI`)." },
  { topic: "Java Programming", diff: "Easy", marks: 2, q: "What is the difference between `final`, `finally`, and `finalize` in Java?", a: "`final` is a keyword to declare constants, prevent method overriding, or prevent class inheritance. `finally` is a block following try-catch that always executes for cleanup. `finalize()` is a deprecated method called by the garbage collector before reclaiming an object.", exp: "final is a modifier, finally is a control block, finalize is an Object lifecycle method.", bet: "`final`: Keyword that makes variables immutable constants, methods non-overridable, and classes non-inheritable. `finally`: Block in exception handling that executes unconditionally (unless `System.exit(0)` is called) to release resources (e.g., closing streams/connections). `finalize()`: Deprecated `Object` method invoked prior to garbage collection, replaced by `AutoCloseable` and try-with-resources." },
  { topic: "Java Programming", diff: "Medium", marks: 3, q: "Explain the differences between HashMap, LinkedHashMap, and TreeMap in Java.", a: "HashMap stores key-values in random/hash order with O(1) average lookup. LinkedHashMap maintains insertion order (or access order) using an internal doubly linked list with O(1) lookup. TreeMap maintains sorted order of keys using a Red-Black Tree with O(log n) lookup.", exp: "HashMap prioritizes speed; LinkedHashMap preserves order; TreeMap guarantees sorted keys.", bet: "Comparison: (1) `HashMap`: Hash table offering O(1) lookup/insert without order guarantees. (2) `LinkedHashMap`: Extends HashMap with a doubly linked list running through all entries, maintaining insertion order (or LRU access order) with O(1) performance. (3) `TreeMap`: Implements `NavigableMap` using a Red-Black Tree, ordering keys according to natural ordering or a custom `Comparator` with guaranteed O(log n) performance for search, insert, and delete." },
  { topic: "Java Programming", diff: "Hard", marks: 5, q: "Explain how Java Memory Model handles volatile variables and how `volatile` differs from `synchronized`.", a: "`volatile` ensures memory visibility and prevents instruction reordering so writes by one thread are immediately visible to all threads, but does not provide atomicity for compound operations (e.g. count++). `synchronized` provides both memory visibility and mutual exclusion (atomicity) by acquiring an intrinsic lock.", exp: "volatile affects CPU caches and memory barriers without thread blocking; synchronized uses mutex locks.", bet: "Under the Java Memory Model (JMM): `volatile` guarantees cache coherence and visibility by inserting hardware memory barriers (flushing CPU write buffers to main memory) and preventing compiler instruction reordering; however, it does not provide atomicity for compound operations like `i++`. `synchronized` acquires an intrinsic monitor lock, ensuring both visibility and mutual exclusion (atomicity), blocking other threads from entering critical sections." },
  { topic: "Operating Systems", diff: "Easy", marks: 2, q: "What is the difference between a Process and a Thread?", a: "A Process is an independent executing program with its own dedicated address space, memory, and resources. A Thread is a lightweight unit of execution within a process that shares memory and resources with other threads of the same process.", exp: "Processes have isolated memory; threads share the process heap and code segments.", bet: "A Process is an isolated execution instance containing its own virtual memory address space, page tables, file descriptors, and security context (heavyweight context switching). A Thread is a lightweight sub-execution stream within a parent process sharing the heap, data, and code segments while maintaining its own program counter, registers, and call stack (fast context switching)." },
  { topic: "Operating Systems", diff: "Medium", marks: 3, q: "Explain the four necessary conditions for a Deadlock to occur (Coffman conditions).", a: "The four Coffman conditions are: (1) Mutual Exclusion (resources cannot be shared), (2) Hold and Wait (processes hold resources while waiting for others), (3) No Preemption (resources cannot be forcibly taken), and (4) Circular Wait (a circular chain of processes each waiting for a resource held by the next).", exp: "Breaking any one of these four conditions prevents deadlocks from forming.", bet: "Deadlocks require all four Coffman conditions simultaneously: (1) Mutual Exclusion: At least one resource is held in a non-shareable mode. (2) Hold and Wait: A process holds at least one resource while waiting to acquire additional resources held by other processes. (3) No Preemption: Resources cannot be forcibly reclaimed; only the holding process can release them voluntarily. (4) Circular Wait: A closed chain of processes exists where each process P_i waits for a resource held by P_{i+1}." },
  { topic: "Operating Systems", diff: "Medium", marks: 3, q: "What is Virtual Memory, and how does Paging with Page Faults work?", a: "Virtual Memory creates an illusion of large contiguous memory by mapping virtual addresses to physical RAM. Memory is divided into fixed-size Pages. When a program accesses a page not currently in physical RAM, the MMU triggers a Page Fault interrupt, causing the OS to load the page from disk into RAM.", exp: "Paging enables memory protection and process isolation.", bet: "Virtual Memory decouples application logical addresses from physical RAM addresses. Virtual address space is divided into fixed-size Pages (e.g. 4KB) mapped to physical Page Frames via Page Tables and the MMU TLB cache. When a referenced page has its valid bit unset in the page table, a Page Fault exception is raised, prompting the OS kernel to locate the page in swap space, evict an old frame (via LRU/FIFO), and update the page table." },
  { topic: "Computer Networks", diff: "Easy", marks: 2, q: "Explain the key differences between TCP and UDP protocols.", a: "TCP (Transmission Control Protocol) is connection-oriented, reliable, guarantees in-order packet delivery with error-checking and flow control, but has higher overhead. UDP (User Datagram Protocol) is connectionless, unreliable, sends packets without delivery guarantees or ordering, but offers low latency suitable for live streaming and gaming.", exp: "TCP uses 3-way handshakes and ACKs; UDP sends raw datagrams without handshakes.", bet: "TCP is a connection-oriented transport protocol using a 3-way handshake (`SYN`, `SYN-ACK`, `ACK`), guaranteeing reliable, ordered byte streams via sequence numbers, checksums, acknowledgments, and congestion control (ideal for HTTP, FTP, SSH). UDP is connectionless and datagram-based without delivery guarantees, retransmissions, or ordering, offering minimal latency and header overhead (ideal for DNS, VoIP, real-time gaming)." },
  { topic: "Computer Networks", diff: "Medium", marks: 3, q: "What happens step-by-step when you type a URL (e.g., https://example.com) into a browser address bar and press Enter?", a: "The browser: (1) Resolves the domain IP via DNS cache/lookup, (2) Initiates a TCP 3-way handshake with the server on port 443, (3) Performs TLS/SSL handshake for HTTPS encryption, (4) Sends an HTTP GET request, (5) Server processes request and returns HTTP response (HTML/CSS/JS), and (6) Browser parses DOM/CSSOM and renders the page.", exp: "Involves DNS, transport layer TCP, TLS security, application HTTP, and browser rendering engine.", bet: "Detailed flow: (1) DNS Resolution: Browser checks local cache, OS hosts file, DNS resolver, root, TLD, and authoritative nameservers to resolve IP. (2) TCP 3-Way Handshake: SYN -> SYN-ACK -> ACK established on port 443. (3) TLS Handshake: Client/Server Hello, certificate verification, key exchange (ECDHE), and symmetric cipher negotiation. (4) HTTP Request & Response: Browser sends HTTP/2 GET; web server/reverse proxy routes request and returns HTTP 200 with HTML payload. (5) Browser Rendering: Parses HTML into DOM, CSS into CSSOM, executes JS, builds Render Tree, computes Layout, and paints pixels." },
  { topic: "Web & Security", diff: "Medium", marks: 3, q: "What is Cross-Site Scripting (XSS) and how can web applications prevent it?", a: "XSS is a security vulnerability where an attacker injects malicious client-side JavaScript scripts into web pages viewed by other users. Prevention includes: contextual output encoding/escaping, sanitizing user input, using Content Security Policy (CSP) headers, and storing authentication tokens in HttpOnly cookies.", exp: "XSS steals session cookies, redirects users, or manipulates DOM content.", bet: "Cross-Site Scripting (XSS) occurs when an application includes untrusted user input in a web page without proper validation or escaping, allowing execution of malicious scripts in the victim's browser context (Stored, Reflected, or DOM-based XSS). Defenses: (1) Context-aware output encoding (HTML, JS, URL encoding), (2) Content Security Policy (CSP) HTTP headers restricting script sources, (3) Setting `HttpOnly` and `SameSite` flags on session cookies to prevent JS access via `document.cookie`, and (4) Using frameworks like React that automatically escape JSX values." },
  { topic: "Web & Security", diff: "Medium", marks: 3, q: "What is SQL Injection (SQLi) and how is it prevented using Prepared Statements?", a: "SQL Injection occurs when untrusted user input is directly concatenated into SQL queries, altering the query logic. It is prevented by using Parameterized Queries (Prepared Statements), where SQL structure is compiled first and user inputs are treated strictly as literal data values rather than executable code.", exp: "Prepared statements separate SQL code from user-supplied parameters at the database protocol level.", bet: "SQL Injection occurs when malicious user input modifies the syntactic structure of an SQL statement (e.g. entering `' OR '1'='1`). It is prevented using Parameterized Queries (Prepared Statements / ORMs). The database engine compiles and optimizes the query template beforehand; user inputs are bound as literal data parameters through separate protocol packets, guaranteeing that user input cannot alter the execution tree or inject SQL commands." },
  { topic: "Software Engineering & Git", diff: "Easy", marks: 2, q: "What is the difference between `git merge` and `git rebase`?", a: "`git merge` combines branches and creates a new merge commit, preserving the complete branching history. `git rebase` moves the base of your branch onto another branch, rewriting commit history to create a clean, linear commit timeline.", exp: "Merge preserves history; rebase linearizes history by reapplying commits.", bet: "`git merge` combines changes from a source branch into the target branch by creating a 3-way merge commit, preserving the exact chronological branching topology. `git rebase` takes commits from the current branch and replays them one by one on top of the target branch's latest commit, rewriting commit hashes to produce a clean, linear project history. Rebase should never be used on shared public branches." },
  { topic: "Software Engineering & Git", diff: "Medium", marks: 3, q: "What is Continuous Integration and Continuous Deployment (CI/CD) in modern software delivery?", a: "Continuous Integration (CI) automatically builds and tests code changes whenever developers commit to a shared repository. Continuous Deployment (CD) automatically deploys all validated changes directly to production environments without manual intervention.", exp: "CI/CD automates testing, validation, and release pipelines to shorten feedback loops.", bet: "CI/CD modernizes software delivery pipelines: (1) Continuous Integration (CI): Automates code integration where every pull request triggers automated building, unit/integration testing, and static analysis (linting, vulnerability scans) to detect defects early. (2) Continuous Delivery: Automatically stages tested builds for release. (3) Continuous Deployment (CD): Automatically deploys all passing builds directly to production infrastructure without manual human gates, enabling rapid, low-risk releases." }
];

// Dynamically generate the remaining high quality technical questions up to 160
let curId = 41;
while (technicalQuestions.length < 160) {
  for (const t of topicsList) {
    if (technicalQuestions.length >= 160) break;
    const qNum = curId++;
    const idStr = String(qNum).padStart(3, "0");
    technicalQuestions.push({
      questionId: `benchmark-tech-${idStr}`,
      question: `[${t.topic}] ${t.q}`,
      expectedAnswer: t.a,
      explanation: t.exp,
      betterAnswer: t.bet,
      difficulty: t.diff,
      marks: t.marks,
      questionType: "Technical",
      options: [],
      correctAnswer: "",
      source: "practice",
      companyId: "benchmark",
      companyIds: ["benchmark"],
      topic: t.topic
    });
  }
}
